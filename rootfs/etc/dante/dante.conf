# Dante Package Manager Configuration
# Abandon all hope, ye who enter here.

BINARY="YES"                         # "YES" prefers verified binary packages
MAKEFLAGS="-j4"                      # Parallel jobs (raise only if you have the RAM)
PARALLEL="4"                         # Concurrent package builds

# Repositories (official first). The last URL is used for `dante sync`.
REPO_URL="https://github.com/AstralZX/antenora-packages.git"
REPO_URLS="https://github.com/AstralZX/antenora-packages.git"

# Dante User Repository (public, anyone can commit)
DUR_URL="https://github.com/AstralZX/antenora-dur.git"

# Binary package mirrors (ordered failover). See `dante mirror`.
BINARY_MIRROR="https://raw.githubusercontent.com/AstralZX/antenora-packages/main/packages"
MIRROR_FILE="/etc/dante/mirrors.conf"

CLEAN_SOURCE="YES"                   # Remove source after build
KEEP_DEPS="NO"                       # Remove dependencies when removing package
