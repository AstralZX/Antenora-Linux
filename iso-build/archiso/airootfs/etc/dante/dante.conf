# Dante Package Manager Configuration
BINARY="NO"                          # Set to "YES" for binary packages
MAKEFLAGS="-j$(nproc)"              # Default make flags
REPO_URL="https://github.com/antenora/package-repo.git"
BINARY_MIRROR="https://cdn.antenora.org/packages"
CLEAN_SOURCE="YES"                   # Remove source after build
KEEP_DEPS="NO"                       # Remove dependencies when removing package
