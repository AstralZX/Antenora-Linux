package "zlib" version "1.3.1" {
    description "zlib compression library"
    source "https://zlib.net/zlib-1.3.1.tar.gz"

    build {
        run "./configure --prefix=/usr"
        run "make -j$HELL_JOBS"
    }

    install {
        run "make install DESTDIR=$HELL_ROOT"
    }
}
