package "libarchive" version "3.7.4" {
    description "Multi-format archive and compression library"
    source "https://github.com/libarchive/libarchive/releases/download/v3.7.4/libarchive-3.7.4.tar.xz"
    depends "zlib" "xz" "zstd" "lz4"

    build {
        run "./configure --prefix=/usr"
        run "make -j$HELL_JOBS"
    }

    install {
        run "make install DESTDIR=$HELL_ROOT"
    }
}
